# Visuels

- `ads-testees/` : les créas réellement diffusées (mapping angle → fichier documenté ici)
- `concurrent/` : les créas du concurrent (veille datée)

⚠️ Documenter les visuels MANQUANTS (médias expirés) — jamais de troncature silencieuse.
