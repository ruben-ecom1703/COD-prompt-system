# Data brute

- `exports-meta/` : le CSV de perf + le XLSX de créatives, bruts, pour re-vérification
- `ads.json` : version machine-readable (généré par le build)

**Règle** : tout chiffre du repo doit être re-calculable depuis ces fichiers.
